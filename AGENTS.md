<!-- VIDA_PORTFOLIO_EXECUTION_START -->
## Vida Big Data execution policy

For work in this registered target or companion, read the shared [execution policy](/Users/nuribatuhan/Documents/Codex-Projects/Vida-AWS-Backend/agent-infos/execution-policy.md) before substantive work. It owns fixed delivery/exclusions/acceptance, cross-task writer and deployment ownership, bounded retries, proportional review/testing, reusable workers, and task-sized research. The [orchestrator](/Users/nuribatuhan/Documents/Codex-Projects/Vida-AWS-Backend/agent-infos/orchestrator.md) is the single source for portfolio model routes.

- Check the central `VidaAI/project-tasks/active-ownership.json` in that control plane before shared mutations; a different worktree does not isolate cloud deployments. Honor paused/reserved handoffs.
- The shared policy supersedes conflicting local workflow/model-count/host-name-only/mandatory-Graphify clauses, including inherited generated instructions. Keep this project's product, security, privacy, release authorization and technically necessary validation contracts.
- Graphify is optional for useful broad structural analysis or explicit `/graphify`; ordinary questions and fixes never require it. No manual graph update or graph freshness gate is required to finish a task. Preserve existing background indexing; do not load full graphs by default.
- This applies to this project's work. It does not change global skills/plugins or impose portfolio rules on unrelated consumer projects.
<!-- VIDA_PORTFOLIO_EXECUTION_END -->

<!-- PROJECT_BRAIN_CONTEXT_ROUTING_START -->
## Technical context routing

Use one navigation source by default, then verify exact claims in source, configuration, serialized state, tests, builds, or runtime evidence.

- Product behavior, UX rules, and accepted requirements: use the Project Brain `Context Index.md` and only the relevant canonical notes.
- File, module, route, config, or test ownership: use a bounded read-only CODE_MAP query when `CODE_MAP.md` exists; never update it implicitly.
- Broad architecture, dependency, data-flow, path, or impact analysis: use an existing Graphify graph only when the task explicitly asks for that analysis or the user invokes `/graphify`.
- Exact symbols and small fixes: use targeted `rg` and direct source inspection.
- Do not query Graphify and CODE_MAP together by default. Escalate to the second only when the first source is insufficient.
- Graphify output and CODE_MAP entries are orientation, not proof. Never let them override accepted Project Brain truth.
- Keep automatic code-only Graphify refresh hooks, but do not load the full graph report or wiki into model context for ordinary work.
<!-- PROJECT_BRAIN_CONTEXT_ROUTING_END -->
